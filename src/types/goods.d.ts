interface ISeach {
    page: number,
    listType: string,
    goodsName: string,
    goodsNo: string,
    categoryId: any,
    per_page: number,
}

type radioInfo = {
    label: string,
    value: number
}